package clouds 
{
	import starling.display.Image;
	import starling.display.Sprite;
	import starling.textures.SubTexture;
	
	/**
	 * ...
	 * @author Marcus Graf
	 */
	public class Cloud extends Image 
	{
		
		
		public function Cloud(texture:SubTexture, isShadow:Boolean, cloudHeight:Number)
		{
			/*super(texture);
			if (isShadow) {
				x = cloudHeight * BootjeTest.HEIGHT_PARALLAX_EXAG;
				y = cloudHeight * BootjeTest.HE
			}
			
			var cloud:Image = new Image(cloudTexture);
			var shadow:Image = new Image(shadowTexture);
			shadow.x = cloudHeight * BootjeTest.HEIGHT_TO_X_OFFSET;
			shadow.y = cloudHeight * BootjeTest.HEIGHT_TO_Y_OFFSET;
			addChild(cloud);
			addChild*/
		}
		
	}

}